NODE_ENV=development
PORT=4000
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# Ruta opcional a un JSON externo con el shape de YouTube videoListResponse.
# Si se omite, se usa src/data/mock-youtube-api.json incluido en el repo.
# MOCK_VIDEOS_PATH=/abs/path/to/mock-youtube-api.json
